import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.henriiconsulting.astroguider',
  appName: 'AstroGuider',
  webDir: 'out', 
};

export default config;
